{
  "name": "quackscript-mono",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "Apps/*",
    "Lib/*"
  ],
  "scripts": {
    "build": "yarn --cwd ./Apps/web build --outDir=../../out/web & yarn --cwd ./Apps/docs build --out-dir ../../out/docs"
  }
}
