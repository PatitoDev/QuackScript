import P from './parser';
import I from './interpreter';
import L from './lexer';

export const Parser = P;
export const Interpreter = I;
export const Lexer = L;

const QuackScript = {
};

export default QuackScript;